import stdio
import sys
#Using sys.argv[1] and sys.argv[2] to grab an input from the user
#Used the addition symbol to combine the input with the strings

stdio.writeln(sys.argv[1] + ' is ' + sys.argv[2] + ' years old.')
